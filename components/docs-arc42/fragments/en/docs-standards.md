## Documentation

`docs/` holds three parts with three audiences — technical documentation (arc42), a user
manual, and a domain knowledge base. Which one a page belongs in is decided by who reads it,
not by who wrote it. The conventions are in [documentation.md](documentation.md).

Documentation describes what is built. Anything not built yet belongs in `spec/`.
