# Dokumentation

Wie die Anwendung heute tatsächlich funktioniert — nicht, wie sie funktionieren soll. Pläne
liegen in `spec/`.

Was hier nicht mehr zum Code passt, ist ein Fehler: entweder in derselben Änderung mitziehen
oder löschen.
